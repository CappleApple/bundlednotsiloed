name: Build 1.20.1-forge

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out Bundled Not Siloed
        uses: actions/checkout@v4
        with:
          path: bundled-not-siloed

      - name: Check out matching Stacks Not Slots port
        uses: actions/checkout@v4
        with:
          repository: CappleApple/stacksnotslots
          ref: cappleapple/1.20.1-forge
          path: stacks-not-slots

      - name: Set up Java 17 for Gradle
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build and test matching projects
        working-directory: bundled-not-siloed
        run: bash ports/build-port.sh 1.20.1-forge test build runGameTestServer --no-daemon

      - name: Upload production JARs
        uses: actions/upload-artifact@v4
        with:
          name: 1.20.1-forge
          if-no-files-found: error
          path: |
            bundled-not-siloed/ports/1.20.1-forge/build/libs/bundlednotsiloed-1.20.1-forge-1.5.1.jar
            stacks-not-slots/ports/1.20.1-forge/build/libs/stacksnotslots-1.20.1-forge-1.1.1.jar

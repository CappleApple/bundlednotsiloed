name: Build 1.21.1-fabric

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out Bundled Not Siloed
        uses: actions/checkout@v4
        with:
          path: bundled-not-siloed

      - name: Check out matching Stacks Not Slots port
        uses: actions/checkout@v4
        with:
          repository: CappleApple/stacksnotslots
          ref: cappleapple/1.21.1-fabric
          path: stacks-not-slots

      - name: Set up Java 21 for Gradle
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build and test matching projects
        working-directory: bundled-not-siloed
        run: bash ports/build-port.sh 1.21.1-fabric test build runGameTestServer --no-daemon

      - name: Upload production JARs
        uses: actions/upload-artifact@v4
        with:
          name: 1.21.1-fabric
          if-no-files-found: error
          path: |
            bundled-not-siloed/ports/1.21.1-fabric/build/libs/bundlednotsiloed-1.21.1-fabric-1.5.1.jar
            stacks-not-slots/ports/1.21.1-fabric/build/libs/stacksnotslots-1.21.1-fabric-1.1.jar

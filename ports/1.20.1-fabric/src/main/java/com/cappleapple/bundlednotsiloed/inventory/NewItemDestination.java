package com.cappleapple.bundlednotsiloed.inventory;

/** Preferred first destination for world pickups whose item identity is not already stored. */
public enum NewItemDestination {
    HOTBAR_FIRST,
    INVENTORY_FIRST,
    STOWED_FIRST
}

package com.cappleapple.bundlednotsiloed.compat;

/** Vanilla result-slot action requested after recipe ingredients are placed. */
public enum RecipeTransferDestination {
    NONE,
    CURSOR,
    INVENTORY
}
